spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DogacUlgacc/microservice-turkcell

          default-label: main

          search-paths:
            - config-server
            - configurations/
            - configurations/{application}

server:
  port: 8787
